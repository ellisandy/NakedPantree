<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleDisplayName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>
    <!-- Build number flows from `CURRENT_PROJECT_VERSION` so CI can
         override per-run (`GITHUB_RUN_NUMBER` in
         `.github/workflows/testflight-beta.yml`). Hardcoding a literal
         here is what caused two silent TestFlight rejections in
         issue #92's wake — every upload landed as build "1", which
         was lower than the build "10" already on file. Local builds
         get `CURRENT_PROJECT_VERSION = 1` from Xcode defaults; CI
         gets the run number. Marketing version
         (`CFBundleShortVersionString`) above stays a literal; bump it
         by hand on milestone changes. -->
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <!--
        Standard export-compliance exemption: Naked Pantree uses only
        Apple-provided system encryption (HTTPS via URLSession,
        CloudKit / Core Data at-rest encryption, Keychain). No
        third-party crypto libraries, no custom cryptography, no
        end-to-end encryption layered on top of the platform.
        Verified by tree grep — no `CryptoKit` / `CommonCrypto` /
        `Security` imports and zero third-party SwiftPM dependencies
        (see Packages/Core/Package.swift). Declaring `false` here
        skips App Store Connect's per-upload export-compliance
        questionnaire. Re-evaluate if the app ever adds bundled
        OpenSSL / libsodium / custom AES / etc.
    -->
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>NSCameraUsageDescription</key>
    <string>Take photos of items in your pantry so you can spot them at a glance.</string>
    <key>NSRemindersFullAccessUsageDescription</key>
    <string>So Naked Pantree can drop your restock list into Reminders when you ask.</string>
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <true/>
    </dict>
    <key>UIBackgroundModes</key>
    <array>
        <string>remote-notification</string>
    </array>
    <!--
        Issue #87 — UIKit's "Update the Info.plist: Launch screens
        will soon be required" runtime warning was firing because
        the previous value was an empty dict, which iOS treats as
        "no launch screen configured". Apple is moving toward
        making `UILaunchScreen` mandatory for App Store submission,
        so this is a release blocker as much as a polish item.

        Color: `BrandWarmCream` from `Assets.xcassets/Brand/`. The
        catalog's `Brand` group has `provides-namespace: false` so
        the launch screen resolves the bare name without needing
        `Brand/BrandWarmCream`. This matches the in-app cream
        background of `LaunchView` (Phase 8.2 splash) — no flash
        on handoff between the OS launch screen and the first
        SwiftUI frame.

        No `UIImageName` yet — the brand wordmark exists as text in
        `LaunchView`, not as an asset image, and the launch screen
        takes a static image not text. Adding an image is a polish
        follow-up; the cream background alone satisfies the
        requirement and avoids the blank-window flash.
    -->
    <key>UILaunchScreen</key>
    <dict>
        <key>UIColorName</key>
        <string>BrandWarmCream</string>
        <key>UIImageRespectsSafeAreaInsets</key>
        <true/>
    </dict>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
</dict>
</plist>
